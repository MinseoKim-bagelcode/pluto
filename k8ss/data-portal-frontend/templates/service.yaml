apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-portal-frontend.fullname" . }}
  labels:
    {{- include "data-portal-frontend.labels" . | nindent 4 }}
    {{- with .Values.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-portal-frontend.selectorLabels" . | nindent 4 }}
